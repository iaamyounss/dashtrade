// leave communicate the database with the graphql api
// create a left menu nav, or a mui stepper to implement the 'just enough to begin'

export default function AdminView() {
  return <p>Hey !</p>
}